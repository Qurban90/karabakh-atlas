# Prod: nginx (docker) proxies /api → server:5001. For a split deploy
# (e.g. Vercel client + Render API) set the absolute API origin instead:
# VITE_API_URL=https://qdx-api.onrender.com/api
VITE_API_URL=/api
